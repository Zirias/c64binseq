MEMORY {
  LDADDR: start = $bffe, size = 2;
  CODE: start = $c000, size = $1000;
}

SEGMENTS {
  LDADDR: load = LDADDR;
  INPUT: load = CODE;
  MAIN: load = CODE;
  TOSTRING: load = CODE;
  OUTPUT: load = CODE;
  BSS: load = CODE, type=bss;
}

